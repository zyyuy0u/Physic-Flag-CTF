' ============================================================
' 系統時序圖 — 防禦監控系統
' ============================================================
' 本檔描述系統內部的監控機制與元件互動流程，
' 不涉及特定攻擊情境（攻擊驗證屬於後續滲透測試階段）。
' ============================================================


' ============================================================
' 圖一：系統啟動與初始化流程
' ============================================================
@startuml 01_system_startup
title 系統啟動與初始化流程

actor "管理員" as Admin
participant "Docker Compose" as DC
database "db\n(MariaDB)" as DB
participant "web-app\n(PHP+Apache)" as Web
participant "defense-system\n(Python)" as Defense
participant "GPIO LED" as LED

autonumber

Admin -> DC: 啟動所有服務
activate DC
DC -> DB: 啟動資料庫容器
activate DB
DC -> Web: 啟動網站容器
activate Web
DC -> Defense: 啟動防禦監控容器
activate Defense
DC --> Admin: 所有容器啟動完成
deactivate DC

== 資料庫初始化（首次部署） ==

Admin -> Web: 執行資料庫初始化腳本
Web -> DB: 建立使用者資料表
DB --> Web: 建立成功
Web -> DB: 新增預設使用者帳號
DB --> Web: 新增成功
Web --> Admin: 初始化完成

== 防禦系統初始化 ==

Defense -> Defense: 透過 docker inspect\n查詢 Web Container 網段

Defense -> Defense: 建立 IP 白名單\n（Loopback + Link-local + Docker 網段）

Defense -> LED: 初始化 GPIO 腳位\n（全部設為熄滅狀態）

Defense -> Defense: 啟動執行緒 A\n（Docker Log 即時監控）
Defense -> Defense: 啟動執行緒 B\n（網路連線即時監控）

note over Defense: 系統就緒\n兩個監控執行緒持續運行中

@enduml


' ============================================================
' 圖二：執行緒 A — Docker Log 即時監控機制
' ============================================================
@startuml 02_thread_a_docker_log_monitor
title 執行緒 A — Docker Log 即時監控機制

participant "web-app\n(Apache)" as Web
participant "defense-system\n（執行緒 A）" as ThreadA
participant "LED1" as LED1
participant "LED2" as LED2

autonumber

ThreadA -> Web: 透過 docker logs -f --tail 0\n建立持續串流連線
activate ThreadA

note over ThreadA: 僅讀取啟動後的新日誌\n以阻塞式逐行讀取

group 迴圈 [即時逐行讀取 Docker Container Log]

  Web --> ThreadA: 串流輸出新的 Log Entry

  ThreadA -> ThreadA: 以 Regex 比對\n偵測規則 A

  group 條件 [偵測規則 A 命中]
    ThreadA -> LED1: 輸出 GPIO HIGH 訊號
    activate LED1
    note over LED1: LED1 常亮（不自動熄滅）
  end

  ThreadA -> ThreadA: 以 Regex 比對\n偵測規則 B

  group 條件 [偵測規則 B 命中]
    ThreadA -> LED2: 輸出 GPIO HIGH 訊號
    activate LED2
    note over LED2: LED2 常亮（不自動熄滅）
  end

end

deactivate ThreadA

@enduml


' ============================================================
' 圖三：執行緒 B — 網路連線即時監控機制
' ============================================================
@startuml 03_thread_b_netstat_monitor
title 執行緒 B — 網路連線即時監控機制

participant "web-app\n(Container)" as Web
participant "defense-system\n（執行緒 B）" as ThreadB
participant "LED3" as LED3

autonumber

ThreadB -> Web: 透過 docker exec 在 Container 內\n啟動持續執行的 netstat 程序
activate ThreadB

note over ThreadB: 每秒執行一次 netstat -tn\n將結果即時串流回主程式

group 迴圈 [每秒取得連線快照]

  Web --> ThreadB: 串流輸出 netstat 連線資料

  ThreadB -> ThreadB: 篩選 ESTABLISHED\n狀態之連線

  ThreadB -> ThreadB: 第一層過濾：\n排除 Local Port 80\n（正常 HTTP 連線）

  ThreadB -> ThreadB: 第二層過濾：\n比對 Remote IP\n是否在 Whitelist 中

  group 條件 [Local Port ≠ 80 且 Remote IP 不在 Whitelist 中]
    ThreadB -> LED3: 輸出 GPIO HIGH 訊號
    activate LED3
    note over LED3: LED3 常亮（不自動熄滅）
  end

end

deactivate ThreadB

@enduml
